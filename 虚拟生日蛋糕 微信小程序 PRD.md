# 《吹吹蛋糕》虚拟生日蛋糕微信小程序 — 产品需求文档 (PRD)

| 项目 | 内容 |
| --- | --- |
| 产品名称 | 吹吹蛋糕（Blow Blow Cake） |
| 产品形态 | 微信小程序 |
| 文档版本 | v1.0（MVP + 分享与定制） |
| 文档作者 | Jiayi |
| 最后更新 | 2026-04-20 |
| 文档状态 | 交付开发评审 |
| 目标读者 | 前端/后端开发、测试、设计 |

---

## 1. 产品概述

### 1.1 背景

远距离、跨时区、忙碌的工作节奏使得朋友之间难以在生日当天亲自到场送上祝福。一条微信文字祝福略显单薄，红包又少了仪式感。需要一种低门槛、高情感浓度、兼具仪式感和互动感的"在线送祝福"方式。

### 1.2 产品愿景

用一块能吹灭的虚拟蛋糕，把"我想陪你过生日"这件事装进一条小程序链接里。

### 1.3 产品定位

轻量级、一次性体验为主的情感互动工具。核心交互是**对手机麦克风吹气，屏幕上的蜡烛随之熄灭，伴随个性化的生日祝福播放**。产品不做社区、不做UGC沉淀，只做"送—收"这一条单向链路。

### 1.4 产品目标（v1）

- 上线30天内累计生成蛋糕祝福卡 ≥ 10,000 张
- 生成者 → 接收者的打开率 ≥ 60%
- 吹蜡烛交互完成率（首次进入页面到蜡烛全灭）≥ 70%
- 接收者二次分享率 ≥ 15%

---

## 2. 目标用户与使用场景

### 2.1 目标用户画像

主要用户为18–35岁、活跃于微信的年轻人，**送方**（发起祝福者）与**收方**（过生日者）同等重要。送方追求"让祝福更特别"；收方追求"被用心对待的惊喜感"。

### 2.2 核心场景

场景一：小李的大学舍友在另一个城市过生日，她在微信里打开小程序，选好蛋糕样式、填上舍友名字和一段语音祝福，生成卡片并转发到对方微信。

场景二：舍友在生日当天收到链接，点开后看到一个写着自己名字的蛋糕，对手机"呼——"地吹了一口气，蜡烛一根根熄灭，屏幕上浮现小李的祝福语和语音，截屏保存纪念，并顺手转发进闺蜜群。

### 2.3 非目标场景（Out of Scope for v1）

企业/品牌营销定制、蛋糕商品电商导流、用户之间长期社交关系沉淀、AI生成祝福文案。

---

## 3. 用户故事（User Stories）

| 编号 | 角色 | 用户故事 | 优先级 |
| --- | --- | --- | --- |
| US-01 | 送方 | 作为送方，我希望选择蛋糕样式和蜡烛数量，以表达个性 | P0 |
| US-02 | 送方 | 作为送方，我希望给蛋糕写上寿星名字和一段文字祝福 | P0 |
| US-03 | 送方 | 作为送方，我希望录一段语音祝福附在蛋糕上 | P1 |
| US-04 | 送方 | 作为送方，我希望上传一张与寿星的合照作为蛋糕背景 | P1 |
| US-05 | 送方 | 作为送方，我希望预览后生成一条可转发的小程序卡片 | P0 |
| US-06 | 收方 | 作为收方，我希望点开链接就能看到属于我的蛋糕 | P0 |
| US-07 | 收方 | 作为收方，我希望对着手机吹气就能把蜡烛一根根吹灭 | P0 |
| US-08 | 收方 | 作为收方，蜡烛全灭后希望看到祝福语并听到语音 | P0 |
| US-09 | 收方 | 作为收方，我希望能保存截图 / 录屏 / 分享到朋友圈或群 | P1 |
| US-10 | 收方 | 作为收方，我希望给送方"回个拥抱"或留一句话 | P2 |

---

## 4. 功能需求

### 4.1 功能地图

v1版本包含四大模块：**蛋糕制作**、**蛋糕观看（吹蜡烛）**、**分享与回礼**、**基础后台与数据**。下面逐模块拆分。

### 4.2 蛋糕制作（送方侧）

#### 4.2.1 蛋糕样式选择

提供6款预置蛋糕样式（草莓、巧克力、奶油水果、抹茶、彩虹、卡通），每款包含不同颜色方案。v1不支持自定义外观，仅支持预设切换。

#### 4.2.2 蜡烛配置

蜡烛数量可选1–99支（超过20支使用"数字蜡烛"样式避免拥挤）。颜色从5种预设中选择。默认根据用户填写的年龄自动生成。

#### 4.2.3 祝福内容

文字祝福：最多200字，支持Emoji。必填字段：寿星昵称（最多20字）、生日日期（用于倒计时与定时解锁，详见4.3.1）。可选字段：语音祝福（最长30秒，mp3/aac，存储于微信云存储）、合照一张（≤5MB，裁剪为16:9或1:1）。

#### 4.2.4 预览与生成

生成前必须进入预览页，完整演示一次吹蜡烛流程。确认后生成唯一`cake_id`，返回可分享的小程序卡片（含自定义封面图、标题、描述）。

### 4.3 蛋糕观看（收方侧）

#### 4.3.1 进入逻辑

通过小程序卡片链接携带`cake_id`参数直达播放页。若当前日期早于蛋糕记录中的`birthday_date`，展示"生日倒计时"页面并锁定主要内容；等到0点自动解锁。若超过`birthday_date + 30天`，展示"蛋糕已保存至回忆"并引导登录查看。

#### 4.3.2 吹蜡烛核心交互

入场动画：蛋糕浮现，寿星名字以手写体写在蛋糕上方，蜡烛依次点亮，播放轻量BGM（可静音）。

交互提示：底部浮出"对着手机吹一吹 🎂"的提示 + 麦克风图标脉动。首次需请求麦克风权限（`scope.record`）。

吹灭判定：实时采集麦克风音频，当**短时能量**（RMS）高于阈值 **且** 持续 ≥ 300ms **且** 低频比例（< 500Hz）占比 ≥ 60%，判定为一次有效"吹气"。每次有效吹气随机熄灭 1–3 根仍然点亮的蜡烛（人数少时每次熄灭1根以保仪式感）。

降级方案：若麦克风权限被拒绝或机型不支持，自动切换为"长按屏幕模拟吹气"模式，保证流程可完成。

#### 4.3.3 揭示祝福

全部蜡烛熄灭后，触发3秒烟花粒子特效，蛋糕缩小至顶部，中部展示文字祝福与合照，底部自动播放语音祝福（带播放控制）。

#### 4.3.4 纪念功能

展示"保存祝福卡片"按钮，将当前页面渲染成一张长图（寿星名字 + 祝福语 + 蛋糕主视觉 + 小程序码），调用`wx.saveImageToPhotosAlbum`保存至本地相册。

### 4.4 分享与回礼

#### 4.4.1 分享给好友 / 朋友圈

支持微信"分享给好友"与"分享到朋友圈单页模式"。自定义封面图由模板拼合寿星名字生成；标题默认"🎂 [昵称]，你的专属生日蛋糕"，描述可由送方自定义。

#### 4.4.2 回礼功能（US-10，P2）

收方可在蛋糕页点击"抱抱 Ta"按钮，向送方回传一条预置表情或自定义文字。送方通过订阅消息获得通知（需预先授权订阅）。v1若时间不足可降级为纯展示入口不通知。

### 4.5 我的页面

展示用户维度下的"我送出的蛋糕"与"我收到的蛋糕"列表，支持查看每张蛋糕的打开次数、吹灭完成情况。数据仅对本人可见。

---

## 5. 非功能需求

### 5.1 性能

首屏（蛋糕观看页）白屏时间 ≤ 1.5s（4G网络），JS主包 ≤ 2MB（微信上限），单张蛋糕相关资源（图片+音频）≤ 3MB。吹蜡烛实时音频处理端到端延迟 ≤ 150ms。

### 5.2 兼容性

微信基础库 ≥ 2.30.0；覆盖iOS 13+与Android 8.0+；需兼容深色模式（至少不反白不错位）；需兼容横屏锁定（主流程仅竖屏）。

### 5.3 可用性

无网络时，已生成的蛋糕资源通过本地缓存支持至少一次完整播放；麦克风权限被拒绝时自动降级到"长按吹气"；资源加载失败时展示友好兜底页并提供重试。

### 5.4 安全与合规

所有用户上传内容（文字、图片、音频）必须经过微信安全能力`security.msgSecCheck` / `security.imgSecCheck` / `security.mediaCheck`审核，不通过的内容禁止生成蛋糕。遵循《个人信息保护法》，首次上传合照时弹出明确授权说明，收方无权下载送方上传的原图（只能保存合成后的长图）。

### 5.5 数据与隐私

用户仅存储openid、unionid（如有）、昵称、头像、created_at、自己上传的蛋糕内容。蛋糕默认有效期30天，过期后对外不可见但后台保留90天用于找回，之后物理删除。

---

## 6. 技术方案

### 6.1 技术栈

前端：微信原生小程序（WXML/WXSS/JS），构建工具使用`miniprogram-ci`。状态管理采用MobX-miniprogram或全局`getApp()`方案，按项目规模选择。动画使用小程序`Canvas 2D` + `requestAnimationFrame`；粒子与3D烟花特效使用`skyline`渲染引擎或Lottie。

后端：微信云开发（CloudBase）作为主后端，使用云函数（Node.js 18）+ 云数据库 + 云存储，零运维；若未来扩展需要，保留一层HTTP网关以便迁移到自建服务。

开发协作：Git（主干分支模型 main / dev / feature-*），代码规范ESLint + Prettier，CI通过GitHub Actions跑lint与单元测试。

### 6.2 总体架构

小程序前端通过微信云SDK直接调用云函数与云数据库；音频处理、分享卡片合成这类计算密集型操作放在云函数；静态资源（蛋糕底图、BGM）托管于云存储并开启CDN加速。前端与后端约定版本号`api_version`字段以便未来灰度。

### 6.3 核心技术实现

#### 6.3.1 麦克风吹气检测

使用小程序`RecorderManager`以`PCM`格式、采样率16000、单声道开启录音；通过`onFrameRecorded`每 100–200ms 回调一帧音频数据。前端实现一个轻量DSP模块：

1. 计算帧RMS能量：`rms = sqrt(mean(sample_i^2))`；
2. 对帧做FFT（使用`dsp.js`或自实现Radix-2 FFT，帧长2048），计算500Hz以下能量占比；
3. 维护滑动窗口，满足"RMS > THRESH_RMS 且 低频占比 > THRESH_LOW 持续3帧"即判定一次吹气；
4. 触发吹灭事件后进入200ms的冷却期，避免重复触发。

阈值默认值建议：`THRESH_RMS = 0.05`（归一化），`THRESH_LOW = 0.6`。需在3款以上不同机型上调参。

#### 6.3.2 蛋糕主视觉渲染

v1使用Canvas 2D + 多图层PNG（蛋糕底、蜡烛、火焰、装饰）合成。火焰使用精灵图动画（6帧）+ 随机轻微位移模拟摇曳。蜡烛熄灭动效：火焰淡出 + 一股烟雾粒子（8个粒子，2秒上升并透明化）。后续版本可升级为skyline 3D。

#### 6.3.3 分享长图生成

收方"保存祝福卡片"流程：云函数接收`cake_id`，使用`node-canvas`基于预设模板拼合文字、合照、小程序码（通过`wxacode.getUnlimited`获取），输出PNG后返回临时URL；前端通过`wx.downloadFile` + `wx.saveImageToPhotosAlbum`保存。

### 6.4 数据模型（云数据库集合）

集合 `cakes`：

```
{
  _id: string,              // cake_id，主键
  ownerOpenid: string,      // 送方openid
  receiverNick: string,     // 寿星昵称（非微信昵称，由送方填写）
  birthdayDate: string,     // YYYY-MM-DD，用于倒计时解锁
  style: {
    theme: "strawberry" | "chocolate" | ... ,
    candleCount: number,    // 1-99
    candleColor: string
  },
  content: {
    text: string,           // <= 200字
    voiceFileID: string?,   // 云存储FileID，可空
    photoFileID: string?    // 云存储FileID，可空
  },
  stats: {
    openCount: number,
    blowCompletedCount: number,
    lastOpenedAt: Date
  },
  audit: {
    textStatus: "pass" | "risky" | "reject",
    imgStatus: "pass" | "risky" | "reject",
    voiceStatus: "pass" | "risky" | "reject"
  },
  createdAt: Date,
  expireAt: Date            // createdAt + 30天
}
```

集合 `users`：`{ _id: openid, unionid, nick, avatar, createdAt, lastActiveAt }`。

集合 `events`（埋点）：`{ _id, userOpenid, cakeId, type, payload, ts }`。

### 6.5 云函数清单

| 函数名 | 作用 | 入参 | 关键逻辑 |
| --- | --- | --- | --- |
| `createCake` | 创建蛋糕 | 蛋糕字段 | 内容安全校验 → 写入`cakes` → 返回`cake_id`与分享参数 |
| `getCake` | 获取蛋糕详情 | `cake_id` | 校验过期与可见性；返回带临时URL的资源链接 |
| `blowDone` | 上报吹灭完成 | `cake_id` | 更新`stats.blowCompletedCount`，触发可选的订阅消息 |
| `renderCard` | 生成长图 | `cake_id` | `node-canvas`合成长图，返回临时URL |
| `hugBack` | 回礼 | `cake_id`, `text` | 内容安全校验，写入子集合，推送订阅消息 |
| `track` | 埋点 | 事件字段 | 异步写入`events` |

### 6.6 错误码约定

`0`成功；`4001`参数缺失；`4003`内容审核未通过；`4040`蛋糕不存在或已过期；`4290`频控（同一openid每分钟最多创建3张蛋糕）；`5000`内部错误。所有返回体统一为 `{ code, msg, data }`。

### 6.7 安全风控

创建频控：同一openid每分钟3张、每天20张。内容审核：所有文本/图片/音频全部走微信内容安全接口，`risky`标记人工复核，`reject`直接拒绝生成。反爬与盗链：云存储资源通过临时URL下发，有效期1小时。

---

## 7. 页面与交互

### 7.1 页面清单

首页（入口与营销）、蛋糕制作页（分4步：样式→蜡烛→祝福→预览）、蛋糕观看页（收方核心页）、祝福揭示页、我的页面、帮助与设置页、失败/兜底页（过期、审核未通过、权限拒绝）。

### 7.2 关键交互流程

制作流程：首页「为TA做蛋糕」→ 样式选择 → 蜡烛配置 → 填写祝福（文字+语音+合照） → 预览 → 生成卡片 → 返回首页并展示"已生成"Toast。

观看流程：点击分享卡片 → 加载动画 → 进入蛋糕观看页 → 请求麦克风权限（首次） → 吹蜡烛 → 蜡烛全灭 → 揭示祝福 → 保存/分享/回礼 → 退出。

### 7.3 视觉与文案要点

主色：奶油粉 `#FFD5CC` 与深巧克力 `#3E2723`；主字体：手写体（微信原生`Huawei LiXingKai`或内置`Song`为兜底）。所有文案需照顾到非创建者也能看懂，收方首次进入时不展示任何广告或引导注册弹窗。

---

## 8. 埋点与指标

### 8.1 关键事件

`page_view`、`cake_create_start`、`cake_create_step`（含step编号）、`cake_create_success`、`cake_open`、`mic_permission_result`、`blow_detected`、`blow_completed`、`wish_revealed`、`save_image`、`share_click`、`hug_back_sent`。

### 8.2 北极星与护栏指标

北极星指标：**分享卡片打开率** = `cake_open UV / cake_create_success PV`。护栏指标：首页到制作页转化率、吹蜡烛完成率、崩溃率（< 0.5%）、云函数平均耗时（< 300ms）。

---

## 9. 版本里程碑

### 9.1 里程碑规划（基于8周开发）

M0 立项与设计（第1周）：需求确认、视觉稿、交互稿评审通过。

M1 核心可玩（第2–4周）：蛋糕制作4步流程、观看页吹蜡烛核心交互、基础云函数`createCake`/`getCake`完成，内部可用。

M2 分享与定制（第5–6周）：语音与合照上传、分享卡片、长图生成、我的页面完成，进入内测。

M3 打磨与上线（第7–8周）：性能优化、机型兼容调优、埋点接入、内容安全接入、灰度发布与正式上线。

### 9.2 发布策略

首发采用微信小程序**灰度发布**：0%→10%→50%→100%，每档观察24小时。关键守护指标：崩溃率、审核通过率、首屏时长。

---

## 10. 风险与应对

### 10.1 麦克风检测不准确

风险：不同机型麦克风灵敏度差异大，阈值难以统一。应对：多机型实测 + 个性化自适应（首次吹气校准一次）；提供"长按吹气"降级。

### 10.2 内容安全审核不通过率高

风险：用户上传含敏感词或不合规图片导致体验中断。应对：前端先做离线敏感词过滤；审核未通过时提供明确修改指引而非简单拒绝。

### 10.3 微信平台政策与上限

风险：小程序主包2MB、每日调用次数、订阅消息模板上限等平台限制。应对：资源分包 + 懒加载；订阅消息仅在回礼关键节点使用；准备备用模板ID。

### 10.4 情感伪造/骚扰风险

风险：恶意用户用他人名字生成蛋糕骚扰。应对：展示"由[送方昵称]送出"；提供"举报并删除"入口（P2）。

---

## 11. 依赖与开放问题

### 11.1 外部依赖

微信开发者资质与小程序注册（类目建议：工具/生活服务）、微信云开发配额、微信内容安全接口配额、ICP备案（若使用自有域名）。

### 11.2 待确认问题

(1) 是否接入「打赏送真实蛋糕券」商业化入口——影响是否需要对接商家SKU，**v1不做**；(2) 是否支持匿名送出——影响用户体系，**v1默认实名（显示送方昵称）**；(3) 是否提供国际版/英文版——**v1仅中文**。

---

## 12. 附录

### 12.1 术语表

送方：发起并生成蛋糕的用户；收方：扫码/点链接打开蛋糕的用户（通常为寿星）；吹气事件：麦克风采集到满足判定条件的一次气流；揭示：全部蜡烛熄灭后展示祝福内容的过程。

### 12.2 参考资料

微信小程序官方文档：<https://developers.weixin.qq.com/miniprogram/dev/framework/>；RecorderManager API：<https://developers.weixin.qq.com/miniprogram/dev/api/media/recorder/RecorderManager.html>；微信内容安全：<https://developers.weixin.qq.com/miniprogram/dev/framework/security.html>。

---

_文档到此结束。评审后进入设计稿与技术方案详设阶段。_
